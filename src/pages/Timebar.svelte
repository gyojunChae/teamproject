<script>
  let min = new Date().getMinutes();
  let hour = new Date().getHours();
  let sec = new Date().getSeconds();
  let time = new Date().getTime();
  let minStr, hourStr;

  setInterval(
    () => (
      (time = time + 1000),
      (min = new Date(time).getMinutes()),
      (hour = new Date(time).getHours() % 12),
      (sec = new Date(time).getSeconds()),
      (minStr = String(min).padStart(2, "0"))
    ),
    1000
  );
</script>

<div class="time-bar">
  <div class="time-bar__time">{hour}:{minStr}</div>
  <div class="time-bar__iconBox">
    <img src="assets/chart-bar.svg" alt="" />
    <img src="assets/wifi.svg" alt="" />
    <img src="assets/battery.svg" alt="" />
  </div>
</div>

<style>
  /*맨 위 시간과 배터리*/
  .time-bar {
    position: fixed;
    top: 10px;
    left: 0px;
    margin-bottom: 5px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 20px;
  }
  .time-bar__time {
    font-size: 12px;
    margin-left: 15px;
    font-weight: bold;
  }
  .time-bar__iconBox {
    margin-right: 15px;
    position: fixed;
    right: 0px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .time-bar__iconBox img {
    width: 14px;
    margin-left: 5px;
  }
</style>
